# Kopie nach .env.server (lokal, nicht committen). Deploy nutzt diese Datei als Vorlage.
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/crm_monteur
POSTGRES_PASSWORD=postgres
JWT_SECRET=change-me-stark-und-einzigartig
API_PORT=3801
WEB_PORT=3800
NEXT_PUBLIC_API_URL=/api
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=HIER_GEHEIMES_PASSWORT
MINIO_BUCKET=crm-documents
MINIO_USE_SSL=false
MINIO_BROWSER_REDIRECT_URL=https://s3.example.com
STORAGE_LOCAL_FALLBACK=false
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM=crm@example.local
